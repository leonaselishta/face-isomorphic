Face Isomorphism Tracker (OpenCV-only version)
Uses Haar cascades + facial landmark detection via dlib OR
falls back to a geometry-based approach with OpenCV's face detector.

Install:
    pip install opencv-python opencv-contrib-python numpy

Run:
    python face_isomorphic.py